apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-ingress-green
spec:
  replicas: 1
  selector:
     matchLabels:
       app: green-app
  template:
    metadata:
      labels:
        app: green-app
    spec:
      containers:
        - name: green-app
          image: devopswithcloudhub/nginx:green #blue-app
          ports:
            - containerPort: 80

---
apiVersion: v1 
kind: Service
metadata:
  name: deploy-ingress-green
spec:
  selector:
     app: green-app
  type: ClusterIP
  ports:
  - targetPort: 80
    port: 80
    protocol: TCP
